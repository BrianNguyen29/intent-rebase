apiVersion: v1
kind: Namespace
metadata:
  name: intent-rebase
  # Applied to GCP project ferrum-497801. Not production-ready: internal smoke
  # deploy only, single-node cluster, no public ingress, no real Slack/SMTP
  # secrets, no A-07 pen test completed.
