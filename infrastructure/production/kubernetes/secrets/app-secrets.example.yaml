apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: intent-rebase
type: Opaque
stringData:
  # TEMPLATE ONLY — not applied; do not claim production-ready.
  # All values must be replaced with real secrets before apply.
  # Do not commit this file with real values.
  database_url: "postgres://CHANGE_ME_DB_USER:CHANGE_ME_DB_PASSWORD@CHANGE_ME_DB_HOST:5432/intent_rebase"
  jwt_secret: "CHANGE_ME_JWT_SECRET"
  api_key: "CHANGE_ME_API_KEY"
  slack_webhook_url: "CHANGE_ME_SLACK_WEBHOOK_URL"
  smtp_host: "CHANGE_ME_SMTP_HOST"
  smtp_port: "587"
  smtp_username: "CHANGE_ME_SMTP_USERNAME"
  smtp_password: "CHANGE_ME_SMTP_PASSWORD"
  smtp_from: "CHANGE_ME_SMTP_FROM"
  smtp_to: "CHANGE_ME_SMTP_TO"
