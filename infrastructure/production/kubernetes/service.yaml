# INTERNAL SMOKE DEPLOY ONLY — not production-ready.
# ClusterIP only; no external ingress or LoadBalancer.

apiVersion: v1
kind: Service
metadata:
  name: intent-api
  namespace: intent-rebase
  labels:
    app: intent-api
spec:
  type: ClusterIP
  selector:
    app: intent-api
  ports:
    - port: 8080
      targetPort: 8080
      name: http
